# Versions the analysis was last run against.
# Older versions of scikit-learn (< 1.0) lack StratifiedGroupKFold.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
seaborn>=0.13

# Optional. Only needed for the SHAP analysis in 08_train_real.py,
# which the paper does not report.
shap>=0.44

# Optional. Only needed for the BiLSTM in 08_train_real.py,
# which performed at chance and is not reported in the paper.
# tensorflow>=2.15
